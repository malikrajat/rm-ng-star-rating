# Product Reviews Scenarios

A detailed example showing how to display a list of product reviews with star ratings.

## Usage
- Aggregated summary score display.
- Iterating through a list of review objects using `*ngFor`.
- Combining the rating component with other UI elements (author, date, text).
